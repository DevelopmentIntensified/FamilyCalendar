/**
 * The pinned pdfjs-dist version — the single source of truth for the CDN
 * worker URL (client PDF extraction) and the server legacy build import.
 * Keep in sync with the pdfjs-dist entry in package.json.
 */
export const pdfjsVersion = '5.4.149';
