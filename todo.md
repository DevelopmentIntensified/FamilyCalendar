Working on my Paper from 4:20 PM to 5 PM. With me is no longer getting the end time. 
clicking the more button on a day when it has more than 3 events doesn't show the other events
the title should be truncated to fit in the event details model and there should be no x scroll
the event details model should show the start/end times and calendar and attendants
the EventFormModal  should have the call select in edit mode and the delete button should work
rsvp updating should work and it should be clear what your rsvp and attendant's rsvp is
deleting an event should immediatly be reflected by the ui
when items are detected, and the user clicks show more then show less, the detected items should be hidden again untill the parsing happens again
flash mob starting at noon and lasting for about 15 min should have the end time as 12:15 and min should be the abbreviation for minutes
use svelte-code-writer and ask any questions you may have before starting
Make sure to tdd with nlp related changes